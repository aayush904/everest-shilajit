{% stylesheet %}
  table {
    width: 100%;
    border-collapse: collapse;
    font-family: var(--font-body-family), ui-sans-serif, system-ui, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', Segoe UI Symbol,
      'Noto Color Emoji';
  }

  th,
  td {
    border: 1px solid #ddd;
    padding: 1rem 1.2rem;
    text-align: left;

    display: flex;
    align-items: center;
  }

  th {
    background-color: #f4f4f4;
    text-align: center;
    display: flex;
    align-items: center;
  }
{% endstylesheet %}

<style>
    #shopify-section-{{ section.id }} {
      {% if section.settings.background_on == "whole" %}
    background: {% if section.settings.background_type == 'color' %}{{ section.settings.background_color }}{% else %}url('{{ section.settings.background_image }}'){% endif %};
    background-size: {{ section.settings.background_size }};
    background-position: {{ section.settings.background_position }};"
    {% endif %}
      }


  #shopify-section-{{ section.id }} .wrapper{
    color:{{ section.settings.content_color }};
    padding: {{ section.settings.section_padding }};
    margin: {{ section.settings.section_margin }};
    {% if section.settings.background_on == "content" %}
    background: {% if section.settings.background_type == 'color' %}{{ section.settings.background_color }}{% else %}url('{{ section.settings.background_image }}'){% endif %};
    background-size: {{ section.settings.background_size }};
    background-position: {{ section.settings.background_position }};
    {% endif %}
    }

    #shopify-section-{{ section.id }} .contentwrapper{
      gap:4rem ; width:100%; max-width: {{ section.settings.section_width }}; min-height: {{ section.settings.section_min_height }}
    }
  @media(max-width:1280px){
    #shopify-section-{{ section.id }} .contentwrapper{
      max-width:1166px;
    }
  }



    @media(max-width:768px){
      #shopify-section-{{ section.id }} .wrapper.wrapper{
        padding: {% render 'mobile_spacing'  padding: section.setting.section_padding %};
      }

      }
</style>
{% assign contents = '' %}
{% for block in section.blocks %}
  {% if block.type == 'heading' %}
    {% if forloop.index0 != 0 %}
      {% assign contents = contents | append: '###' %}
    {% endif %}

  {% else %}
    {% assign final_content = '' %}
    {% assign icon = block.settings.table_content_icon | replace: '"', "'" %}
    {% if block.settings.table_content_url != blank %}
      {% assign final_content = "<a class='flex justify-start items-center gap-[10px] cursor-pointer text-border' href="
        | append: block.settings.table_content_url
        | append: '>'
        | append: block.settings.table_content
        | append: icon
        | append: '</a>'
      %}
    {% else %}
      {% assign final_content = block.settings.table_content %}
    {% endif %}

    {% if forloop.last %}
      {% assign contents = contents | append: final_content %}
    {% else %}
      {% assign contents = contents | append: final_content | append: '***' %}
    {% endif %}
  {% endif %}
{% endfor %}

{%- assign rows = contents | split: '***###' -%}
{%- assign result_array = '' -%}

{%- for row in rows -%}
  {%- assign items = row | split: '***' -%}
  {%- assign result_array = result_array | append: items | append: '|' -%}
{%- endfor -%}

{%- assign final_array = result_array | split: '|' -%}

<div
  class="main-wrapper wrapper page-width text-image-slider p-4 flex items-center justify-center {{ section.settings.custom_class }}"
  {% if section.settings.custom_id != blank %}
    id="{{ section.settings.custom_id }}"
  {% endif %}
>
  <div
    class="flex flex-col items-center justify-center  sm:max-w-[100%!important] contentwrapper"
  >
    <!-- Main Section Heading and Description -->
    <div
      class="main-content sm:max-w-[100%!important]"
      style="
        {%- if  section.settings.alignment == 'left' -%} text-align: left;
        {%- elsif  section.settings.alignment == 'center' -%} text-align: center;
        {%- else -%} text-align: right; {%- endif -%} width:100%; max-width: {{ section.settings.content_part_width }};
      "
    >
      <h2 class="text-tertiary">{{ section.settings.main_heading }}</h2>
      {{ section.settings.main_content }}
    </div>

    <div class="table-wrapper">
      <table border="1" class="dynamic-table">
        <thead>
          <tr>
            {%- for block in section.blocks -%}
              {%- if block.type == 'heading' -%}
                <th>{{ block.settings.table_heading }}</th>
              {%- endif -%}
            {%- endfor -%}
          </tr>
        </thead>
        <tbody>
          {% assign max_counter = 0 %}

          {% for item in final_array %}
            {% assign array = item | split: ',' %}
            {% if array.size > max_counter %}
              {% assign max_counter = array.size | minus: 1 %}
            {% endif %}
          {%- endfor -%}

          {% for i in (0..max_counter) %}
            <tr>
              {% for item in final_array %}
                {% assign cleaned_text = item | remove: '["' | remove: '"]' | strip | strip %}
                {% assign cleaned_text = cleaned_text | split: '", "' %}
                <td>{{ cleaned_text[i] | strip | replace:'\n','' | replace:'\"',''}}</td>
              {%- endfor -%}
            </tr>
          {%- endfor -%}
        </tbody>
      </table>
    </div>
  </div>
</div>
{{ section.settings.custom_liquid }}
<script>
  document.querySelectorAll('.table-wrapper tr').forEach(function (row) {
    // Get all the td elements of the current row
    let allTds = row.querySelectorAll('td,th');

    // Check if all td elements are empty
    let isEmpty = Array.from(allTds).every(function (td) {
      return td.textContent.trim() === ''; // Check if td content is empty after trimming whitespace
    });

    // If all td elements are empty, remove the row
    if (isEmpty) {
      row.remove();
    }
  });
</script>

{% schema %}
{
  "name": "Table Basmil",
  "class": "table-basmil",
  "settings": [
    {
      "type": "header",
      "content": "Contents"
    },
    {
      "type": "html",
      "id": "main_heading",
      "label": "Section Heading"
    },
    {
      "type": "richtext",
      "id": "main_content",
      "label": "Section Description"
    },
    {
      "type": "header",
      "content": "Content alignment"
    },
    {
      "type": "select",
      "id": "alignment",
      "label": "Content Alignment",
      "default": "left",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ]
    },
    {
      "type": "select",
      "id": "valignment",
      "label": "Content Verticle Alignment",
      "default": "top",
      "options": [
        {
          "value": "top",
          "label": "top"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "bottom",
          "label": "bottom"
        }
      ]
    },
    {
      "type": "text",
      "id": "content_part_width",
      "label": "Content Part Width"
    },
    {
      "type": "text",
      "id": "section_padding",
      "label": "Section Padding"
    },
    {
      "type": "text",
      "id": "section_margin",
      "label": "Section Margin"
    },
    {
      "type": "text",
      "id": "custom_class",
      "label": "Custom Class"
    },
    {
      "type": "text",
      "id": "custom_id",
      "label": "Custom ID"
    },
    {
      "type": "text",
      "id": "section_width",
      "label": "Section Width",
      "info": "eg: 50% | 50rem | 50px"
    },
    {
      "type": "text",
      "id": "section_min_height",
      "label": "Section Min Height"
    },
    {
      "type": "select",
      "id": "background_type",
      "label": "Background Type",
      "options": [
        {
          "value": "color",
          "label": "Color"
        },
        {
          "value": "image",
          "label": "Image"
        }
      ]
    },
    {
      "type": "select",
      "id": "background_on",
      "label": "Background On",
      "options": [
        {
          "value": "content",
          "label": "Content"
        },
        {
          "value": "whole",
          "label": "Whole Section"
        }
      ]
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color"
    },
    {
      "type": "color",
      "id": "content_color",
      "label": "Content Color",
      "default": "#000"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "select",
      "id": "background_position",
      "label": "Background Position",
      "options": [
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "top",
          "label": "Top"
        },
        {
          "value": "bottom",
          "label": "Bottom"
        }
      ]
    },
    {
      "type": "select",
      "id": "background_size",
      "label": "Background Size",
      "options": [
        {
          "value": "cover",
          "label": "Cover"
        },
        {
          "value": "contain",
          "label": "Contain"
        }
      ]
    },
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "Custom Html/Liquid"
    }
  ],
  "blocks": [
    {
      "type": "heading",
      "name": "Table Heading",
      "settings": [
        {
          "type": "html",
          "id": "table_heading",
          "label": "Heading",
          "default": "Default Heading"
        }
      ]
    },
    {
      "type": "content",
      "name": "Table Content",
      "settings": [
        {
          "type": "html",
          "id": "table_content",
          "label": "Content for Heading",
          "default": "Default Content"
        },
        {
          "type": "url",
          "id": "table_content_url",
          "label": "Link",
          "info": "Add a link to the content else leave it empty"
        },
        {
          "type": "html",
          "id": "table_content_icon",
          "label": "Link Icon",
          "info": "Add an icon to the link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Table Basmil"
    }
  ]
}
{% endschema %}
